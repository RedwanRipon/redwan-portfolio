'use client';

/**
 * Phase 3: opens a WebSocket to the FastAPI backend, streams mic audio,
 * receives JSON ({speech, route, highlight_id}) + TTS audio frames.
 * Stubbed for now.
 */
export function WebSocketClient() {
  return null;
}
