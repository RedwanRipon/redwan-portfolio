"""LangChain agent. Phase 2 Step 3 fills this in.

Filled in next commit:
- A tool-using agent built on GPT-4o-mini.
- Tools: search_cv_tool (RAG over ChromaDB) + navigation_tool (route
  + optional highlight_id picked by the LLM).
- Returns a ChatResponse-shaped dict.
"""
