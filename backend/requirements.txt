# Loose lower-bound pins so pip picks recent versions that ship
# Python 3.13 wheels. Tested combination uses numpy 2.x (the old
# numpy 1.26 has no Python 3.13 wheel and was forcing a source build).

# --- Web framework ---
fastapi>=0.115
uvicorn[standard]>=0.32
python-dotenv>=1.0
pydantic>=2.9
pydantic-settings>=2.5

# --- LLM / RAG stack ---
langchain>=0.3.13
langchain-openai>=0.2.10
langchain-chroma>=0.1.4
langchain-community>=0.3.13
chromadb>=0.5.20
openai>=1.54
tiktoken>=0.8

# --- Document parsing ---
pypdf>=4.3

# --- Voice (Phase 3): file uploads + audio I/O ---
# FastAPI needs python-multipart to parse multipart/form-data (used by
# the /voice/chat endpoint when the browser uploads recorded audio).
python-multipart>=0.0.9

# --- Database (Phase 4): SQLModel ORM + Postgres driver ---
# sqlmodel = Pydantic + SQLAlchemy combined, written by FastAPI's
# author. psycopg[binary] is the pure-Python Postgres driver — the
# [binary] extra ships prebuilt wheels so Render doesn't need libpq
# system packages.
sqlmodel>=0.0.22
psycopg[binary]>=3.2

# --- Admin auth (Phase 4): JWT signing for cookie sessions ---
PyJWT>=2.9
