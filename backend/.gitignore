# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
*.egg
.venv/
venv/
env/
.python-version

# --- Caches / tooling ---
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.tox/

# --- Env / secrets ---
.env
.env.local
*.pem
*.key

# --- Vector DB persistence ---
# Committed on purpose so the Docker image ships with a populated
# index. Re-run scripts/refresh_cv.py locally + commit the updated
# chroma_db whenever the CV changes.

# --- Editors / OS ---
.vscode/
.idea/
.DS_Store
Thumbs.db

# --- Logs ---
*.log
